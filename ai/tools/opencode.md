# OpenCode Adapter

OpenCode should treat `ai/` as the default project harness.

Recommended read order:

1. `ai/context/project.md`
2. `ai/instructions/repository-rules.md`

When changing behavior, keep the backend, frontend, OpenAPI contract, and
Terraform deployment surface in sync.
